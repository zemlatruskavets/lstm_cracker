from setuptools import setup, find_packages

setup(name='lstm_cracker',
      version='1.0',
      url='https://github.com/zemlatruskavets/lstm_cracker',
      packages=find_packages(exclude=('tests', 'docs')))
